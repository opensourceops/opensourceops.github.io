# Release, containers and remediation documentation execution

## Starting sources and ownership

Framework main was pulled at `cddf1cd0634616cca34f6a7a43acf19c500631c7` and documentation main at `d64d62b7e4277d8e7e48203900697a8397298ccb`. Work continues on `codex/release-containers-remediation` and `codex/release-remediation-docs`. Earlier review branches, evidence and artifacts remain preserved.

This documentation work owns the site source, templates, imports, navigation, validation and later exact-source generated assets. Approved framework prose ownership covers README; product, limitations, compatibility and container guides; installation, first-use, CI, secret-reference and troubleshooting prose; the cookbook index and existing twenty tutorial READMEs. Release implementation, provider configuration, the new remediation example and framework execution ledger remain with their assigned owners. No publication, deployment or paid provider call is part of this documentation work.

## Evergreen contract

- Public introductions, titles, navigation and tutorials no longer use maturity labels or product release numbers as branding.
- Functional workflow/protocol identifiers, dependency pins, fixture refs, source SHAs, checksums and internal runtime metadata remain intact.
- Source installation remains pinned to the exact framework revision. Stable release links are used for native downloads. Published image usage is conditional on actual publication; local source builds cover unreleased changes.
- Five historical execution imports are replaced with short evergreen pages at the same routes. They link to the preserved source records and current operational guidance. Historical source files are unchanged.
- The homepage keeps source provenance but no longer prints the runtime version as product branding. The importer no longer regenerates retired labels.

## Initial validation

Three focused branding-contract tests passed. They reject retired product labels and the old registry placeholder, preserve API identifiers, dependencies, digests and fixture refs, and distinguish visible HTML text from machine metadata. Running the new artifact guard against the earlier built site correctly failed on its visible branding; the diagnostic is retained under the task evidence directory. This is regression evidence, not a passing new-source site build.

The final framework source pin, generated downloads, image invocations and full paired build remain pending implementation integration. No earlier green check is applied to this task's changed source.

## Container integration decisions pending implementation

The minimal image retains the agentctl entrypoint. The proposed tooling image adds a shell, Python and Git under the same image repository, without a Docker socket or GitHub publisher token. The implemented targets are `minimal` (the default final target) and `tooling`, both with the agentctl entrypoint and default UID/GID 65532. Tooling includes a shell, Python, Git and the required packaging/YAML modules. Exact final image identities and runnable platform evidence remain pending integration. The demo runs in its own repository; trusted outer CI owns build, scan and publication effects that are outside agentctl replay.

Official documentation inspected for this increment: [Docker run syntax and image references](https://docs.docker.com/engine/containers/run/), [GitHub stable release links](https://docs.github.com/en/repositories/releasing-projects-on-github/linking-to-releases), and [Harness Run step settings](https://developer.harness.io/3k-docs/continuous-integration/use-ci/run-step-settings/). Harness command scripts require their selected shell and executables in the execution image. Vendor syntax review does not establish hosted Harness execution.

## Next gates

1. Integrate the implemented minimal/tooling image contract and the complete remediation package/export interface.
2. Update maintained container, CI and secret-reference commands, then validate their actual local journeys and declared vendor scope.
3. Pin the reviewed final framework commit, regenerate imports/downloads and run freshness, source, branding, writing, schema, artifact, links, anchors, search, Mermaid and responsive browser/accessibility checks.
4. Push paired draft PRs and preserve exact tested source and hosted artifact identities. The maintainer performs the actual release and public deployment separately.

## Container authoring checkpoint

The container guide now provides a complete credential-free greeting workflow with an external variable file, a policy-authorized report mount, direct inspection and networkless replay. Reviewed configuration is mounted under `/workspace/config`; the state and report host directories are not also exposed through the workspace read mount. Host-owned private writable directories use an explicit matching non-root UID/GID, while the image default remains unchanged.

GitHub Actions guidance uses a hosted-runner Docker invocation and digest-selected image. Harness guidance uses a tooling image with `Sh` and no nested Docker, explicitly labeled vendor syntax review. Secret-file guidance keeps secrets and state outside the source workspace. The architecture, environment/path reference and deployment diagram now match this file-origin contract; ADR 0007 retains its original decision with a current implementation clarification.

Preliminary authoring evidence is retained in the task evidence directory: five YAML blocks parse without duplicate keys, seven standalone shell blocks pass `sh -n`, and the complete first-run workflow passes `check` using the existing local debug executable. Four changed guide files pass Markdown lint. These checks do not claim execution by the forthcoming image or hosted CI. The new independently exported remediation tutorial and final exact-source paired build still await their implementation artifacts.

## Preliminary image execution

Both local ARM64 development images passed the authored image workflow through Podman as host UID/GID `502:20`, with no network, read-only root/configuration and separate private writable state/report directories. The minimal image ID is `04ebbda8366f48ad80538569b3c1f5e3bef7739232bdabee48c5ad031ad61640`; tooling is `efafef5df678e9e83c93e72a84212caa36aa988ff0e64ed946e0b79d8a3ac2b3`. These are dirty development images and are not final-source release evidence.

Version, check, plan, run and inspect passed. After removal of the external variable file, offline replay returned the same output and preserved the exported file's hash and modification time. Replay inspection contained zero effects, provider sessions and tool calls. The tooling shell additionally verified non-root identity, read-only input/root paths, writable state/reports, absence of a workspace state alias, Git, and the Python packaging/YAML modules.

The machine contract observed is `RunOutcome.data.output` (singular), with `runId`, `traceId` and `state`; `RunInspection.data` includes `effects`, `providerSessions` and `toolCalls` arrays. The first evidence-driver attempt incorrectly asserted an `outputs` key after successful runs. Those failed harness attempts are preserved; the corrected second attempts passed. No provider request was made. The task evidence directory contains both attempts, exact commands and streams, a reusable final-image walkthrough driver, and the consolidated `development-image-walkthroughs.json` report.

## Independent remediation download integration

The website now invokes the canonical remediation exporter with `--framework-sha`, `--output` and `--archive`. Its ZIP and one-entry catalog live under `downloads/remediation`; the original twenty-package catalog and validation inventory remain unchanged. The new tutorial has a stable route, navigation and learning-path entries, with dedicated download, search and accessibility assertions.

The artifact verifier checks the independent ZIP's complete application/workflow/adapter/CI file set, regular portable paths, manifest digests, trusted framework SHA, bootstrap repository identities and standalone README links. Package `sourceDirty` describes the package's narrower source scope; site `dirty` describes the whole framework checkout. Dirty previews remain explicitly labeled, and a clean site cannot contain a dirty package. Final sync and execution of these artifact/browser checks await the completed tracked tutorial and final framework source.

The minimal image additionally rejected `/bin/sh`, `/usr/bin/python3` and `/usr/bin/git` as entrypoints with exit `127` and missing-file diagnostics, matching the image-choice table. These negative probes were credential-free and networkless and are recorded separately from the successful tooling inventory.

The literal tooling-entrypoint example also passed. An adverse check using the retired `/config` mount failed before execution with `Error`, exit `2`, `schema_violation` at `spec.varsFiles[0]`, and null run/trace IDs because the external variable source escaped the workspace. Its first probe expected policy exit `3`; that expectation failure is retained. The reviewed observation asserts the exact source-validation exit `2` and diagnostic rather than rerunning an already observed denied operation.

## Integrated source checkpoint: framework 617e1a3

The site was synchronized against clean framework `617e1a3a3ef91e40ca2289d86654b1beacaa35a6`, importing 80 source pages and building separate catalogs for the original twenty examples and the new remediation package. The current framework draft is [PR 8](https://github.com/opensourceops/agentctl/pull/8).

The required paired command stopped in framework `docs-verify`: `examples/v1/reusable-pack.yaml` retained a lock generated for the previous engine version. The exact CLI diagnostic was isolated with provider credentials removed from that check process. This is a failing paired result; the framework owner is correcting the lock and integrating the explicit live-preflight operation before providing another source pin.

Independent site components passed on this checkpoint: generated freshness, writing/branding, Mermaid, workflow pins, Markdown, spelling across 96 files, Astro checks across 28 files with zero errors/warnings, build, external links, Pagefind, all 94 HTML routes/links/anchors, and both complete source/digest-verified download catalogs. Initial browser startup without elevated loopback access failed with `EPERM`; the required browser phase was then run with authorized local-server access.

The browser phase passed 140 checks in 2.6 minutes. One existing generic mobile-search matrix case remains skipped; the dedicated twenty-cookbook and independent-remediation search checks passed on mobile, as did their download, layout and accessibility checks. Desktop remediation and mobile container/table/YAML screenshots were also inspected. The visible product branding is evergreen, source SHA provenance is retained, and code stays horizontally scrollable and copyable. No site deployment or paid call occurred.

Logs and screenshots are retained in the new task's `docs` evidence directory, including `paired-617e1a3-attempt1.log`, `site-617e1a3-attempt1.log` and `browser-617e1a3-attempt1.log`. These passing site components do not change the failed paired verdict or establish final-source evidence. The next integrated framework SHA will be pinned and regenerated before the required final paired verification.

## Integrated source checkpoint: framework 732fef4

Framework `732fef49fc8cde02e75f8e9d50974fe6cc816e9b` corrected the reusable-pack lock and example-runner inventory. Its full `cargo xtask docs-verify` passed inside the paired command. The site regenerated 80 imports and both download catalogs, including mandatory preflight workflow/helper files. The preflight tutorial describes the recorded 8,000-token ceiling; older 3,000-token fixture evidence is not reused as current proof.

The paired command passed source freshness, writing/branding, Mermaid, workflow pins, Markdown, spelling, Astro/build and all 94-page artifact/link/anchor/package checks, then failed on an authentication-only repository settings URL returning HTTP 404. The framework release-guide owner was asked to replace that public link with a repository link and the Settings navigation breadcrumb. The external-link assertion remains unchanged. The log is `paired-732fef4-attempt1.log`; this is not a passing paired run. Browser checks were not repeated on the already failed checkpoint.

The mandatory Markdown command now explicitly includes the five maintained compatibility/history explanation pages under `concepts` and `reference`, matching their earlier focused checks. This keeps those preserved routes inside CI coverage after their historical generated imports were removed.

## External-link transport correction

The paired command at clean framework `46a8923328e5730fe508140b419c130d2a2884e8` passed framework documentation verification and site checks through external URL classification, then remained alive because the fetch transport retained two TCP socket resources. Diagnostic attempts reproduced the leak, including an attempted Connection-close header. The original command and two diagnostic copies were terminated only after their exact commands and working directories were verified. Their exit `143` logs remain failed or interrupted evidence; no paired pass is claimed.

The checker now uses unpooled native HTTP(S) requests, destroys each response after reading its status and retains the existing overall 15-second timeout, redirect bound, TLS defaults and status classifications. Four focused real-server tests cover redirects, HTTP 404, an unfinished response body, timeout and redirect cycles without forcibly closing server connections. All passed. The actual 171 exact-source objects and 16 external URLs then passed and the checker exited normally. Logs are `external-status-tests.log`, `external-native-46a8923.log` and `stopped-link-check-processes.json`; prior diagnostic logs are preserved.

The next paired source pin is framework `f54ccd6e3f96e10ea57bc5585f80b3e68a92aaab`, which also contains the framework release preparation provenance correction. The demo bootstrap pull request is distinct from a model-produced remediation pull request. Live remediation evidence remains with the framework owner and is not inferred from the website build.

## Passing paired checkpoint: framework f54ccd6

The full `AGENTCTL_REPO=... pnpm verify:agentctl` command passed against clean framework `f54ccd6e3f96e10ea57bc5585f80b3e68a92aaab` with pnpm 11.9.0. Framework documentation verification, generated freshness, writing/branding, Mermaid, workflow pins, Markdown, spelling, Astro checks/build, all 94 HTML pages and their links/anchors, both download inventories, external source/URL checks and search passed. The external checker exited normally, allowing the browser phase to run in the same command.

Responsive browser checks passed 140 cases in 2.5 minutes. One unchanged generic mobile-search matrix case is skipped; the dedicated original twenty-package and independent remediation mobile search/download/accessibility checks all passed. The paired log is `paired-f54ccd6-attempt1.log`; `checkpoint-f54ccd6.json` records its hash and source metadata. No provider call, production publication or Pages deployment occurred.

The framework owner is preparing a Windows test-only correction. This successful source checkpoint remains preserved, and the final documentation pin and generated source assets will advance together if that correction changes the reviewed framework SHA.

## Reviewed source: framework 68e5b8e

Clean framework `68e5b8e738f099487c7af9fe1b043ab2c1a5d0b0` adds a Windows mount-construction test correction and execution evidence after the preceding checkpoint. The site workflow pin, 80 imports and all twenty original plus one independent download packages were regenerated together. The complete local paired command passed again, including 140 responsive browser cases in 2.5 minutes and the same one existing generic mobile-search skip. Dedicated mobile search, download, copy and accessibility assertions passed. `paired-68e5b8e-attempt1.log` is the exact-source log.

The documentation source is ready for a draft pull request paired with framework PR 8. The PR workflow validates the exact source and uploads its complete site ZIP; its deployment job is excluded from pull-request events. Hosted verification and the uploaded ZIP digest are recorded separately after that run completes. The demo bootstrap PR is not a model-generated remediation PR, and this documentation result supplies no live-provider or production-publication claim.

## Hosted paired proof and actual uploaded artifact

[Documentation draft PR 7](https://github.com/opensourceops/opensourceops.github.io/pull/7) tests documentation source `35d15cd26d693d21f67d9e13992fa96b72784c2c` with framework `68e5b8e738f099487c7af9fe1b043ab2c1a5d0b0`. [Hosted run 34052645754](https://github.com/opensourceops/opensourceops.github.io/actions/runs/34052645754) passed the full paired command in the validation job (8 minutes 26 seconds). Its browser phase passed 140 cases in 5.1 minutes with the unchanged single generic mobile-search skip. All dedicated mobile package/search/accessibility checks passed. The hosted checkout verified 166 exact source objects locally and the remaining 21 external URLs without a definite failure; the deeper local checkout verified 171 objects and 16 URLs. Deployment and production Pages upload were skipped.

The actual uploaded `agentctl-pages-validation` ZIP, artifact ID `9995138253`, is 4,365,690 bytes. Its SHA-256 is `4467a92dbaa8bb9abeaf49472fe6d16a4469c9f0e105a6486cbad9965ec89572`, identical to the Actions artifact digest. Independent verification confirms `.nojekyll`, clean exact framework metadata, all 94 HTML pages without visible source boilerplate or retired public branding, twenty original cookbook ZIPs and the independent remediation ZIP. Every offered package is byte-identical to the locally validated source package; all nested file digests and source-bound manifests pass. This proves packaging and website relationships, not execution of the paid remediation journey.

The artifact expires on 20 September 2026 at 18:52:18 UTC. The retained evidence directory contains the actual ZIP, Actions metadata, complete hosted log and `hosted-68e5b8e/verification.json` (SHA-256 `ecf430184ec1dfefce9f466027151230179b7b8b598386cd8f29fa81ff6e0bd4`). Its package records include ZIP and manifest hashes for independent comparison with framework execution evidence. The evidence-only documentation commit following `35d15cd` changes this ledger alone; tested site source and the framework pin remain unchanged. Only writing, Markdown and diff checks are repeated for that record.

## Corrected archive source: framework ce00ab9

Framework `ce00ab9ad600bbe6f95e75ef8cf4d800efc67bba` corrects OCI archive metadata, adds its focused regression, and verifies preparation attestations. The documentation pin and every source-bound import/download were refreshed together; tutorial behavior is unchanged. The earlier tested documentation `35d15cd` / framework `68e5b8e` and its evidence-only head `eb88cdd` remain historical passing checkpoints, including their original uploaded ZIP.

The full local `AGENTCTL_REPO=... pnpm verify:agentctl` passed on clean `ce00ab9` with pnpm 11.9.0. Framework documentation checks, freshness, writing, Mermaid, workflow pins, Markdown, spelling, Astro, 94-page routes/links/anchors, both package catalogs, external-source/URL checks and search passed. Responsive browser tests passed 140 cases in 2.5 minutes with the unchanged single generic mobile-search skip; dedicated package/mobile checks passed. The new log is `paired-ce00ab9-attempt1.log`. Hosted validation and its actual ZIP are recorded separately after updating PR 7.

## Hosted proof for corrected archive source

[Hosted run 34055218810](https://github.com/opensourceops/opensourceops.github.io/actions/runs/34055218810) passed the complete paired validation for documentation `4d527f9050e6932301b31b413fc9dbd4b29de112` and framework `ce00ab9ad600bbe6f95e75ef8cf4d800efc67bba`. The validation job completed in 8 minutes 16 seconds; browser checks passed 140 cases in 4.9 minutes with the unchanged single generic mobile-search skip. All dedicated mobile checks passed. Framework verification, source freshness, 94-page artifact/routes/links/anchors, both catalogs, search and external checks passed; the hosted checkout verified 166 exact source objects and 21 external URLs. Deployment and production Pages upload were skipped.

The actual uploaded artifact `9995863202` is 4,365,713 bytes with SHA-256 `78bd1eb18341279255a7f43999b761529eace1bde3341c1ace2de2c53b2a9895`, matching the Actions digest. Independent verification confirms `.nojekyll`, clean framework source metadata, all 94 HTML pages without visible source boilerplate or retired branding, and all twenty original plus one independent package. Every offered ZIP is byte-identical to its locally validated source package, and every nested file digest and source-bound manifest passes. The artifact expires on 20 September 2026 at 19:40:51 UTC.

The new actual ZIP, complete hosted log and Actions metadata are retained under `hosted-ce00ab9`; its `verification.json` has SHA-256 `f2e8beba0eee5fc99a5d5e109ff1593a7a161a3a5d97352169eee7c56f821060` and includes per-package manifest hashes. Earlier reports and ZIPs remain unchanged. The subsequent documentation commit records this evidence only; tested site source `4d527f9` and the `ce00ab9` framework pin remain fixed. Writing, Markdown and diff checks cover that ledger-only change. These site and packaging results make no paid-remediation or production-publication claim.

## Registry probe source: framework b91dc4d

Framework `b91dc4dbc8300bc12809d9d667e07a5ae7465f8f` adds scoped local image-reference cleanup for Docker classic-store probes, three regressions and execution evidence after `ce00ab9`. The site pin, imports and downloadable package identities were regenerated together without tutorial behavior changes. All earlier `ce00ab9` and `68e5b8e` evidence, including actual uploaded ZIPs, remains preserved.

The complete local paired command passed against clean `b91dc4d` with pnpm 11.9.0: framework documentation verification; generated freshness; writing, Mermaid, workflow, Markdown, spelling and Astro checks; all 94 HTML pages, routes, links and anchors; both package catalogs; external-source/URL checks; search; and 140 responsive browser cases in 2.5 minutes. The unchanged single generic mobile-search skip remains; dedicated mobile package checks passed. `paired-b91dc4d-attempt1.log` records this source-specific result. Hosted validation follows the PR 7 update, with its uploaded ZIP checked separately.

## Preserved hosted b91dc4d checkpoint

[Hosted run 34057208999](https://github.com/opensourceops/opensourceops.github.io/actions/runs/34057208999) passed the full paired gate for documentation `133e8f2f3bd570b6f9c454466ddf9f806ff42b14` and framework `b91dc4dbc8300bc12809d9d667e07a5ae7465f8f`. Validation completed in 8 minutes 19 seconds; browser checks passed 140 cases in 5.0 minutes with the unchanged single generic mobile-search skip. Deployment was skipped. Automatic approval review temporarily blocked artifact metadata/log reads after reaching its usage limit. Following the user's request to continue after quota clearance, the original reads succeeded through the same approval path.

Actual artifact `9996447744` is 4,365,850 bytes with SHA-256 `5fc5aa6b3e037018f479ea3027de1c9d8525c514bae7eb0666b1552180d13d38`, matching Actions. Independent verification confirmed `.nojekyll`, clean exact-source metadata, 94 HTML pages without retired branding or source boilerplate, and all twenty original plus one independent ZIP and nested manifest/file digests. The actual ZIP, hosted log and `hosted-b91dc4d/verification.json` remain retained; the verification JSON hash is `01dea3957a6c71ac6c62b0b2ffcdc72c57d966717f3b51b4aad54f74edda13ae`. Its artifact expires on 20 September 2026 at 20:19:42 UTC. This checkpoint was preserved before the framework owner's next test-only correction.

## Timestamp regression source: framework d3f4338

Framework `d3f4338a7735ff610947c1232ebf7797f584993d` changes only the release-budget test's identity assertion/tied-timestamp fixture and its execution ledger after `b91dc4d`. The documentation source pin and every source-bound import/download were regenerated together. The complete local paired command passed: framework documentation verification, generated freshness, writing/Mermaid/workflow/Markdown/spelling/Astro checks, 94-page artifact/routes/links/anchors, both package catalogs, external checks, search, and 140 responsive browser cases in 2.5 minutes with the unchanged single generic mobile-search skip. `paired-d3f4338-attempt1.log` records this exact clean-source result.

The local site remediation ZIP is byte-identical to the separately prepared clean framework export: archive SHA-256 `6d10813ad09b54ae75a6e7ecf6ea6ee4b4fb797c2961a70e1fb2497e4b8760ff`, manifest SHA-256 `4c52252da1c30660110b35bf50eb78b79782eabfbb5032c80ff08d98d68d6d99`. `site-clean-export-local-d3f4338.json` records that direct file comparison. Hosted paired validation and its actual uploaded ZIP follow the PR 7 update; local packaging equality does not establish execution of the paid remediation journey.

## Hosted final-source documentation proof

[Hosted run 34092386026](https://github.com/opensourceops/opensourceops.github.io/actions/runs/34092386026) passed the complete paired gate for documentation `87ff6cf135fe5a1cec6efddbc92953439e1ae660` and framework `d3f4338a7735ff610947c1232ebf7797f584993d`. Validation completed in 8 minutes 17 seconds; 140 browser cases passed in 4.9 minutes with the unchanged single generic mobile-search skip. Dedicated mobile search/download/accessibility checks passed. The run also passed framework verification, source freshness, writing/build, 94-page artifact/routes/links/anchors, both catalogs and search; its shallow checkout verified 166 exact source objects and 21 remaining external URLs. Deployment and production Pages upload were skipped.

Actual uploaded artifact `10007514654` is 4,365,857 bytes with SHA-256 `dccf9777467c25bcb5e4804efb67a8eccf14119527d1825e037a065ac75fcc31`, matching Actions. Independent verification confirms `.nojekyll`, clean exact framework metadata, all 94 HTML pages without visible source boilerplate or retired branding, and all twenty original plus one independent package. Every offered ZIP is byte-identical to its locally validated source package; every nested file digest and source-bound manifest passes. The remediation ZIP extracted from this actual hosted artifact is also byte-identical to the separately prepared clean framework export, SHA-256 `6d10813ad09b54ae75a6e7ecf6ea6ee4b4fb797c2961a70e1fb2497e4b8760ff`.

The artifact expires on 21 September 2026 at 06:55:30 UTC. The actual ZIP, complete log and Actions metadata are retained under `hosted-d3f4338`; its `verification.json` has SHA-256 `71342c3c9aef0f1efd9875c5a01664c19919d78e59703a1271390a9e10e3c02f` and includes every package's manifest hash. `site-clean-export-hosted-d3f4338.json` records the direct clean-export byte comparison. Earlier evidence remains untouched. The following documentation commit changes this ledger alone, preserving tested site source `87ff6cf` and framework pin `d3f4338`; only writing, Markdown and diff checks are repeated for that record. No paid-remediation or production-publication claim follows from these site/package results.

## Provider schema correction: framework 42922f9

Framework `42922f9479f9b9cda360c36cf9b113ae4ce50f9a` replaces the remediation tools' multiline content enums with escaped exact-content patterns while preserving reviewed paths, bytes and authority. It adds explicit cache read/write prices to the full and preflight workflows and corrects first-draft release preparation. The site pin, imports and download packages advance together. The preceding documentation `87ff6cf` / framework `d3f4338` validation, evidence-only documentation head `669f265`, and original uploaded artifact remain historical records.

[Actual preflight run 34155607816](https://github.com/Ompragash/agentctl-remediation-demo/actions/runs/34155607816) passed on framework `d3f4338` using `gpt-6-astra`, high reasoning and `store: false`: one echo tool call, strict output and an independently inspected keyless, networkless replay with zero fresh effects/provider requests. The original receipt records 2 provider requests, 275 tokens and USD 0.003990 estimated. This proves the preflight operation on its recorded source; it does not prove the corrected full workflow.

[Full remediation run 34156165704](https://github.com/Ompragash/agentctl-remediation-demo/actions/runs/34156165704) completed real analysis and deterministic plan validation, then failed before either write tool because OpenAI rejected newline-containing string enums in the strict tool input schema. No model patch or remediation PR was produced. Its original receipt remains unchanged; the framework execution ledger separately records the cache-write pricing correction. The new schema passed exact positive writes, 19 rejected-input cases with no writes, selective repair and zero-effect replay using the actual CLI with synthetic scanner/model fixtures. Successful live execution of the corrected full workflow remains pending and is not inferred from these local contracts or the site build.

The complete local `AGENTCTL_REPO=... pnpm verify:agentctl` passed against clean `42922f9` with pnpm 11.9.0: framework docs verification, generated freshness, writing/Mermaid/workflow/Markdown/spelling/Astro checks, 94-page artifact/routes/links/anchors, both catalogs, external links and search. Responsive browser checks passed 140 cases in 2.4 minutes with the unchanged single generic mobile-search skip; dedicated mobile package, download and accessibility checks passed. The exact-source log is `paired-42922f9-attempt1.log`.

The local site remediation ZIP is byte-identical to the clean framework export: archive SHA-256 `5e3a884988d00f5bbadc3b35de4e284bb7fd9398f58458292b1dcd5e6f3c926b`, manifest SHA-256 `55ec4e49b8b5dba02bd55e9f2926f9d6796476e58ab1e7a6007365762aba7e74`. All 33 manifest-listed payload files pass their individual hashes; `site-clean-export-local-42922f9.json` records the comparison. Hosted validation and its actual preview artifact follow the PR update. Pages deployment approval remains pending; this task only updates the existing documentation PR branch.

## Preserved hosted 42922f9 checkpoint

[Hosted run 34158259258](https://github.com/opensourceops/opensourceops.github.io/actions/runs/34158259258) passed the complete paired gate for documentation `a66879fe34f2ddc0492ff0cd4ee01eb250bc22da` and framework `42922f9479f9b9cda360c36cf9b113ae4ce50f9a`. Validation completed in 8 minutes 22 seconds; 140 browser checks passed in 4.9 minutes with the unchanged single generic mobile-search skip. Dedicated mobile package checks passed. The hosted checkout verified 166 exact source objects and 21 remaining external URLs. Deployment was skipped.

Actual artifact `10031874727` is 4,367,712 bytes with SHA-256 `0121198f90b10efc4a6cb2a4307e00c541b137c4e7fad3339e372de69638b81b`, matching Actions. Independent verification confirmed `.nojekyll`, 94 HTML pages without retired branding or source boilerplate, exact clean framework metadata, and all twenty original plus one independent ZIP with every nested manifest/file digest. Its remediation ZIP is byte-identical to the clean framework export. The artifact expires on 21 September 2026 at 20:16:52 UTC. The retained `hosted-42922f9/verification.json` has SHA-256 `8dbe67f13e42efed4c595e878a819289bc48cea7389d7ac0e6fda3f4b33a51b9`; the actual ZIP and complete hosted log remain alongside it. No source or ledger commit was made solely for this collection.

## Multiline preflight correction: framework ed60436

[Full remediation run 34158074861](https://github.com/Ompragash/agentctl-remediation-demo/actions/runs/34158074861), using framework `42922f9`, failed before either write because OpenAI explicitly rejected regex lookaround. The earlier website, runtime and JavaScript checks did not establish provider acceptance. Both failed full runs and the successful original simple-echo preflight remain preserved with their source identities and original receipts; none is relabeled as success for the corrected workflow.

Framework `ed604369fb73a3d1bba65d8a2026927f59b14e97` uses simple start/end anchors. The pinned Rust validator treats its default end anchor as absolute end; actual CLI fixtures accepted the exact reviewed bytes and rejected all 19 malformed input/path cases without writes. The stronger preflight passes both actual write-input schemas as a typed manifest/lock object through one pure echo. It verifies exact recorded input/result and strict final output while retaining the existing model, budgets and absence of write authority. Focused tests and the actual CLI echo/replay passed. Live execution of this stronger preflight and the corrected full workflow remain pending; the earlier simple-echo success does not satisfy those gates.

The documentation pin, source imports and complete downloads were regenerated together from clean `ed60436`. The full local `AGENTCTL_REPO=... pnpm verify:agentctl` passed with pnpm 11.9.0: framework docs checks, freshness, writing/Mermaid/workflow/Markdown/spelling/Astro checks, 94 HTML pages with routes/links/anchors, both catalogs, external links and search. Browser checks passed 140 cases in 2.4 minutes with the unchanged generic mobile-search skip; all dedicated mobile package checks passed. The exact log is `paired-ed60436-attempt1.log`.

The site remediation ZIP matches the separately prepared clean export byte for byte: SHA-256 `eadad255992e5a7e11453a0c3da416c1f276c36885cf86f6d83242359c5eccd4`, manifest SHA-256 `a54b25fb6848969a22582f33ec9b89108765bb6b64508813ad2c22608e1a5f9f`; all 33 payload hashes pass. `site-clean-export-local-ed60436.json` records the comparison. Hosted validation and its actual artifact follow this PR branch update. Pages approval remains pending; no merge or deployment is attempted.

## Hosted multiline-preflight source proof

[Hosted run 34159970364](https://github.com/opensourceops/opensourceops.github.io/actions/runs/34159970364) passed the full paired gate for documentation `a98ddc9bfa13bfcec2ef78be34af71f18abfb73a` and framework `ed604369fb73a3d1bba65d8a2026927f59b14e97`. Validation completed in 8 minutes 34 seconds; browser checks passed 140 cases in 5.2 minutes with the unchanged generic mobile-search skip. All dedicated mobile package checks passed. The hosted checkout verified 166 exact source objects and 21 remaining external URLs. Deployment and production Pages upload were skipped.

The actual uploaded artifact `10032423421` is 4,369,206 bytes with SHA-256 `d52730fe9cd5fa74aefca88ebad831420fa9e16671160236d91aa68817a8fe33`, matching Actions. Independent verification confirmed `.nojekyll`, 94 HTML pages without retired branding or source boilerplate, exact clean framework metadata, and all twenty original plus one independent ZIP with every nested manifest/file digest. Every package matches its locally validated source package. The hosted remediation ZIP is also byte-identical to the separate clean framework export, SHA-256 `eadad255992e5a7e11453a0c3da416c1f276c36885cf86f6d83242359c5eccd4`.

The artifact expires on 21 September 2026 at 20:43:42 UTC. Its ZIP, complete hosted log and metadata remain under `hosted-ed60436`; `verification.json` has SHA-256 `e5988a5e326bdd2ff96c9135dfa4ed1d701e569110323b1dccea23bad3e7c7e6`. `site-clean-export-hosted-ed60436.json` records the independent clean-export comparison. This subsequent ledger-only commit preserves tested docs source `a98ddc9`, framework pin `ed60436` and all package bytes; only writing, Markdown and diff checks are repeated locally. These site results do not establish paid-provider acceptance or a successful remediation PR.

## Exact content lengths: framework 9640102

The stronger preflights [34159738279](https://github.com/Ompragash/agentctl-remediation-demo/actions/runs/34159738279) and [34160634920](https://github.com/Ompragash/agentctl-remediation-demo/actions/runs/34160634920) accepted the plain anchored schemas, but both returned the same 65-character prefix of the reviewed 158-character lockfile. Runtime validation rejected both responses before the pure echo dispatched. These failed attempts do not establish successful strict output or replay; their original evidence and reconciled receipts remain preserved.

Framework `9640102855bc513e2849d0a08344c1f13e50028a` adds explicit minimum and maximum lengths equal to the reviewed 104-character manifest and 158-character lockfile. Exact anchored patterns, file bytes, permissions, model options and budgets remain unchanged. The final additional change gives the scripted preflight fixture its own filename after a reproduced local shared-mount visibility issue; production provider inputs are unchanged. Focused tests and the local CLI contract passed, including the exact echo and all 19 rejected writes. A fresh bounded live preflight remains pending at this checkpoint, and no successful remediation PR is claimed.

The documentation source pin, imported pages and complete packages advance together to this clean framework source. The full local paired verification passed with pnpm 11.9.0: framework docs checks, generated freshness, writing, Mermaid, workflow, Markdown, spelling, Astro, artifact structure, links, anchors and search. Browser checks passed 140 cases in 2.3 minutes with the unchanged generic mobile-search skip; every dedicated mobile package check passed. The retained log is `paired-9640102-attempt1.log`.

The local remediation ZIP is byte-identical to the separately prepared clean framework export: SHA-256 `8f87ed212d0d06dabc03900f4a1597d43697593810a637b157f72fc23420bbce`, manifest SHA-256 `73d8a5491db6361b53104246ba7dcff1439f557ce6c4510ded7ab0757d91d024`. All 33 payload hashes pass; `site-clean-export-local-9640102.json` records the comparison. Hosted validation and actual artifact inspection follow this PR update. Prior `ed60436` documentation and native-image evidence remain historical source-specific passes. Public Pages merge approval remains pending.

## Unavailable usage accounting: framework c223d01

The preceding documentation checkpoint `b4e83b8d1f7ae12df8280d2b0c466e09f77cd285` remained local and unpushed after [live preflight 34161943315](https://github.com/Ompragash/agentctl-remediation-demo/actions/runs/34161943315) returned a response normalized as `max_tokens` with a configured 512-token ceiling, without a tool call or visible output. The classification defect described below means this does not prove output-cap exhaustion. Its normalized zero counters do not prove zero billed usage or missing raw API counters. The full original three-request, 8,000-token, 120-second, USD0.20 lease remains reserved; the original receipt was not rewritten or reconciled.

Framework `c223d012727a75de62112923d4da8befbe2068f2` rejects missing or malformed required OpenAI usage counters before accepting output. The dedicated error preserves the uncertain model effect and active reservation. Explicit zero remains valid; automatic retry and same-run resume cannot repeat the unresolved request. Existing explicit terminal retry/repair can start separately budgeted model work while preserving the source uncertainty. This defensive change does not establish what the preceding live response actually contained.

The preflight now permits 2,048 output tokens within the unchanged total request, token, time and cost ceilings. It retains the exact content patterns and lengths and the pure echo's absence of write authority. The newly leased preflight remains pending at this source checkpoint; no completed remediation PR is claimed. The documentation pin, imports and all complete downloads are regenerated from this clean source for paired local and hosted verification. Public Pages merge approval remains pending.

The full local paired command passed with pnpm 11.9.0, including the framework documentation gate, generated freshness, static checks, external links, search and all 94 HTML pages. Browser checks passed 140 cases in 2.5 minutes with the unchanged generic mobile-search skip; dedicated mobile package checks passed. The retained log is `paired-c223d01-attempt1.log`.

The site remediation ZIP matches the clean framework export byte for byte: SHA-256 `7c4c0d556a94460914a3b507b4a82fbc3c37713a13deab57cd04be647efaf83d`, manifest SHA-256 `7ebf1a42e31bfa91ace5de9af7a74d44c3f0560fd0df7f85d0b9682e44fc28e3`. All 33 payload hashes pass; `site-clean-export-local-c223d01.json` records the comparison. Hosted validation and actual artifact proof follow the branch push; neither site validation nor the scripted image contracts establish successful paid-provider execution.

[Hosted run 34163785156](https://github.com/opensourceops/opensourceops.github.io/actions/runs/34163785156) passed for docs `e097691afbc42e753a87b660b799c8dc71431d95` and framework `c223d01`: 140 browser checks in 4.8 minutes, one unchanged skip, and validation in 8 minutes 5 seconds. Deployment was skipped. The actual uploaded artifact `10033618736` matches Actions SHA-256 `85238a40c2123e519f8543e19c4b775eed8b017ad7553c9d455dd27d2d675465`. Independent verification confirmed `.nojekyll`, all 94 HTML pages, exact source metadata, all 21 nested packages and every manifest/file digest. The hosted remediation ZIP is identical to the clean export. Report `hosted-c223d01/verification.json` has SHA-256 `7f9933d5d6e7c6f0782e155e17f81340d86e157d41926f566f85ec9da6ad56c3`. This remained a source-specific checkpoint after the next live failure.

## Incomplete response classification: framework 0d4542c

[Preflight 34163342194](https://github.com/Ompragash/agentctl-remediation-demo/actions/runs/34163342194) returned an empty incomplete response. Its strict parser proves the required usage counters were present as explicit numeric zero; the verified receipt records one provider request, zero reported tokens, four seconds and USD0. This is a provider report, not an invoice assertion. The earlier unknown run's full lease remains reserved.

The adapter had classified every incomplete response as `max_tokens`, discarding its reason. Neither failed preflight therefore proves output-cap exhaustion or content filtering. Framework `0d4542cccdbd22cb96e3dec25cdffb66d0938ced` distinguishes an explicit token-limit reason, a content-filter reason, and unspecified incompleteness. It settles known usage and persists the response before failing; incomplete tool and continuation fragments cannot dispatch tools or discard known usage through parsing errors. Existing serialized finish values remain compatible. The focused provider, runtime, replay and compatibility checks passed. No provider request, schema, pricing or output ceiling changed in this increment.

The pin, imported guidance and complete packages advance together to this clean source for paired verification. The next live diagnostic is blocked on approval to merge the demo maintenance PR onto its trusted branch. Public Pages deployment and the separate release-tag correction also remain approval-dependent; this documentation branch update performs none of those actions.

The complete local paired verification passed with pnpm 11.9.0, including framework documentation checks, generated freshness, static validation, external links, search and all 94 HTML pages. Browser checks passed 140 cases in 2.5 minutes with the unchanged generic mobile-search skip; all dedicated mobile package checks passed. The exact-source log is `paired-0d4542c-attempt1.log`.

The site remediation ZIP is byte-identical to the separate clean framework export: SHA-256 `a912893d5a2c674f1800ed02c3a482cb3a017efd95f5204af3251e13f0445fcc`, manifest SHA-256 `ba0c8bcb05c38127bf936c0d7048a624f3db7cac4d241549e49f21597ab312b5`. All 33 payload hashes pass; `site-clean-export-local-0d4542c.json` records the comparison. Hosted validation and actual uploaded artifact proof follow this branch update. These site and package results do not establish successful live remediation.

## Hosted incomplete-response source proof

[Hosted run 34165365905](https://github.com/opensourceops/opensourceops.github.io/actions/runs/34165365905) passed the full paired gate for documentation `60d4bfacc31a582acff7b59dfd41d2ae73a29833` and framework `0d4542cccdbd22cb96e3dec25cdffb66d0938ced`. Validation completed in 8 minutes 2 seconds; 140 browser checks passed in 4.8 minutes with the unchanged generic mobile-search skip. Dedicated mobile package checks passed. The hosted checkout verified 166 exact source objects and 21 remaining external URLs. Deployment and production Pages upload were skipped.

The actual uploaded artifact `10034106393` is 4,371,118 bytes with SHA-256 `ab6db1a1a59cc275fc060d2c2b05cde6026fdcae67dba75091a40eb2c17c541d`, matching Actions. Independent verification confirmed `.nojekyll`, all 94 HTML pages without retired branding or source boilerplate, exact clean source metadata and all 21 nested packages with every manifest/file digest. Every package matches the locally validated source package. The hosted remediation ZIP is also byte-identical to the separate clean framework export, SHA-256 `a912893d5a2c674f1800ed02c3a482cb3a017efd95f5204af3251e13f0445fcc`.

The artifact expires on 21 September 2026 at 22:12:17 UTC. Its actual ZIP, complete hosted log and Actions metadata remain under `hosted-0d4542c`; `verification.json` has SHA-256 `2665b95691d650cbae3a626ec1a61a5208d98a834387fcd6803abb6f553ced0e`. `site-clean-export-hosted-0d4542c.json` records the clean-export comparison. This subsequent ledger-only commit preserves tested documentation source `60d4bfa`, framework pin `0d4542c` and all package bytes; only writing, Markdown and diff checks are repeated locally. No successful live-remediation or public-deployment claim follows from these site results.

## Approved Pages deployment checkpoint

The user approved the pending public Pages action. Documentation PR7 merged as `cff5095b0957459af5646f04050c4e2a046f1c38`, whose file tree matches evidence head `583b406`. [Normal Pages run 34182285736](https://github.com/opensourceops/opensourceops.github.io/actions/runs/34182285736) passed validation in 8 minutes 23 seconds and deployed in 8 seconds. Its 140 browser checks passed in 5.0 minutes with the unchanged generic mobile-search skip. The public site advanced from framework `4a22f7f` to `0d4542c`.

All 92 public `/agentctl/` HTML pages, source metadata, both catalogs and 21 ZIPs matched the actual Pages artifact, including every package payload hash. All 346 deployed files also matched the previous validated PR artifact byte for byte. The official pinned Pages uploader excludes hidden files, so its deployment TAR omits `.nojekyll`, while the generated build and PR ZIP retain it. The first independent harness assumed the same hidden-file inventory and failed; that attempt is retained, and the corrected report explicitly records the packaging distinction. No other file changed or disappeared. Public search and the mobile tutorial passed a browser smoke; no visible source boilerplate or retired branding was present. Report `deployed-0d4542c/deployment-evidence.json` has SHA-256 `1eae9aa433b85e83d95a83c6c9fba1c60e518c85bdd3cace399c0fa30f955b68`.

The browser review found that the imported tutorial still claimed a 512-token preflight output ceiling while its actual workflow specified 2,048. This was a stale source sentence, not a deployment or cache mismatch. It is corrected in the next framework source together with the reviewed output-limit change below.

## Proven preflight output limit: framework bb8fb0a

[Live preflight 34182372619](https://github.com/Ompragash/agentctl-remediation-demo/actions/runs/34182372619) used framework `0d4542c` and failed before the pure echo. Its corrected adapter retained the explicit upstream reason `max_output_tokens`, establishing output-limit exhaustion for this attempt. Its durable response contained no tool call or visible output. Required usage counters were explicitly numeric zero; the verified receipt records one request, zero reported tokens, three seconds and USD0. That provider report is not an invoice assertion. The earlier unknown run 15 still retains its entire original lease. No successful full remediation or model-generated fix PR is claimed.

Framework `bb8fb0a5a28d876fee26c49c49a8a5bf1c8390e2` raises the preflight response ceiling to 4,096 tokens and reserves a fresh three-request, 16,000-total-token, 90-second, USD0.60 allowance within the existing task envelope. The full implementer also receives 4,096 output tokens because it uses the same exact multiline schemas; that choice is an explicit inference, not successful implementer evidence. The analyzer remains at 2,048 output tokens, and the full four-request, 20,000-token, USD1 allowance is unchanged. Model, high reasoning, exact schemas and write authority remain unchanged. The source README now agrees with these limits. Focused contract and adapter tests passed; current-source OCI, hosted and live outcomes remain separate gates.

This documentation branch starts from the merged Pages source and advances the exact framework pin, imports and complete packages together. Historical source-specific passes and failures remain intact. The user has approved normal documentation publication for this task; this update prepares a new draft PR, with merge coordinated by the primary agent after source and live validation.

The complete local paired gate passed with pnpm 11.9.0: framework documentation verification, generated freshness, writing, Mermaid, workflow pins, Markdown, spelling, Astro, artifact/routes/links/anchors, exact-source packages, external links and search. All 140 browser cases passed in 2.5 minutes with the unchanged generic mobile-search skip. The retained log is `paired-bb8fb0a-attempt1.log`.

The offered remediation ZIP is identical to the separately prepared clean export: SHA-256 `5853c6b30e97fb5efe179f21953ce4a2a6bfd8fc20823ce8aca55e86d05838c6`, manifest SHA-256 `484405cefad100ed27efac3586ad593f0934495a5cd929dd0cc9214ae8f13b7e`. All 34 current payload files pass their individual hashes. The workflow uses 4,096 output tokens and the stale 512-token prose is absent. `site-clean-export-local-bb8fb0a.json` records the comparison. Hosted paired validation and actual artifact inspection follow the draft PR update; these results do not establish successful live remediation.

## Hosted revised-output source proof

[Hosted run 34184232135](https://github.com/opensourceops/opensourceops.github.io/actions/runs/34184232135) passed the full paired gate for documentation `826071bf92f0bf975757f27db6201b22f686dd59` and framework `bb8fb0a5a28d876fee26c49c49a8a5bf1c8390e2`. Validation completed in 8 minutes 33 seconds; 140 browser checks passed in 5.1 minutes with the unchanged generic mobile-search skip. Dedicated mobile package checks passed. The hosted checkout verified 166 exact source objects and 21 remaining external URLs. Deployment and production Pages upload were skipped.

The actual uploaded artifact `10040098155` is 4,375,370 bytes with SHA-256 `c069d43f044e062cdf6f0e2eabcb171ecc29ce1e57fd3ad22009a5c94d93abf6`, matching Actions. Independent verification confirmed `.nojekyll`, all 94 HTML pages without retired branding or source boilerplate, exact clean source metadata and all 21 packages with every nested manifest/file digest. Every package matches its locally validated source package. The hosted remediation ZIP also matches the separate clean framework export byte for byte, including all 34 payload hashes.

The artifact expires on 22 September 2026 at 03:47:25 UTC. Its actual ZIP, complete hosted log and Actions metadata remain under `hosted-bb8fb0a`; `verification.json` has SHA-256 `60ba75abe63693c493c5e25d35eb739f797bad2f2d6bdc0dc430505644cb24ba`. `site-clean-export-hosted-bb8fb0a.json` records the clean-export comparison. This subsequent ledger-only commit preserves tested documentation source `826071b`, framework pin `bb8fb0a` and all package bytes; only writing, Markdown and diff checks are repeated locally. Successful live preflight and full remediation remain separate gates; this source has not yet been deployed by this documentation update.
